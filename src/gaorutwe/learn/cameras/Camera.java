package gaorutwe.learn.cameras;

public class Camera {

    // we have fields that store values i.e. data
    private boolean active = false;
    String serialNumber;

    // we have methods that do something or get values i.e. actions (getting the activity)

    boolean getActive() {
        return active;
    }
}
